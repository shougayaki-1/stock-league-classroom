import { Button, Stack, TextField, Typography } from '@mui/material'
import { NumberInput } from '../../../../lessonInputs/NumberInput'
import { SingleChoiceInput } from '../../../../lessonInputs/SingleChoiceInput'
import type { WizardStepProps } from '../../GuidedBuilderWizard'
const defaults = { mainObjective: '', lessonDurationMinutes: 50, studentCount: 30, deviceEnvironment: 'ONE_PER_STUDENT', teamMode: 'TEAM', readingDepth: 'STANDARD', theme: '', difficulty: 'STANDARD', companyCount: 5, useEarnings: true, useUncertainty: false, infoVsDemandWeight: 'BALANCED', alwaysOnMarketMinutes: 20, predictionCheckpoints: 2, evaluationFocus: 'OPERATION_RESULT' }
export function SocialStudiesQuestionStep({ value, onChange, onNext, onBack }: WizardStepProps) { const a = { ...defaults, ...value }; const set = (patch: object) => onChange({ ...a, ...patch }); return <Stack spacing={2}><Typography variant="h6">授業の内容を教えてください（社会科）</Typography><TextField label="主な学習目標" value={String(a.mainObjective)} onChange={(e) => set({ mainObjective: e.target.value })} /><TextField label="テーマ" value={String(a.theme)} onChange={(e) => set({ theme: e.target.value })} /><NumberInput id="lesson-duration" label="授業時間（分）" config={{ type: 'NUMBER', min: 10, max: 200 }} value={a.lessonDurationMinutes as number} errors={[]} onChange={(lessonDurationMinutes) => set({ lessonDurationMinutes })} /><NumberInput id="company-count" label="企業数" config={{ type: 'NUMBER', min: 2, max: 12 }} value={a.companyCount as number} errors={[]} onChange={(companyCount) => set({ companyCount })} /><SingleChoiceInput id="difficulty" label="難易度" config={{ type: 'SINGLE_CHOICE', options: ['BASIC', 'STANDARD', 'ADVANCED'] }} value={a.difficulty as string} errors={[]} onChange={(difficulty) => set({ difficulty })} /><SingleChoiceInput id="sensitivity" label="情報と需給の重視" config={{ type: 'SINGLE_CHOICE', options: ['INFO_FOCUSED', 'BALANCED', 'DEMAND_FOCUSED'] }} value={a.infoVsDemandWeight as string} errors={[]} onChange={(infoVsDemandWeight) => set({ infoVsDemandWeight })} /><Stack direction="row" spacing={1}><Button onClick={onBack}>戻る</Button><Button variant="contained" onClick={() => { onChange(a); onNext() }}>次へ</Button></Stack></Stack> }
