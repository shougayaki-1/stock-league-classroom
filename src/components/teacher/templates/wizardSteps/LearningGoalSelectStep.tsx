import { Card, CardActionArea, CardContent, Chip, Stack, Typography } from '@mui/material'
import type { LearningGoal } from '../../../../lib/lessonTemplates/guidedBuilderTypes'
const choices: { goal: LearningGoal; title: string; description: string; subject: string }[] = [{ goal: 'MARKET_AND_INVESTING', title: '市場のしくみ・投資判断を学ばせたい', description: '企業・ニュース・需給から売買を判断します。', subject: '社会科' }, { goal: 'LIFE_PLANNING', title: '将来に向けた資産形成・生活設計を学ばせたい', description: '家計の設計とライフイベントに向き合います。', subject: '家庭科' }]
export function LearningGoalSelectStep({ onSelect }: { onSelect: (goal: LearningGoal) => void }) { return <Stack spacing={2}><Typography variant="h6">今回の授業で何を学ばせたいですか？</Typography><Stack direction={{ xs: 'column', sm: 'row' }} spacing={2}>{choices.map((choice) => <Card key={choice.goal} sx={{ flex: 1 }}><CardActionArea aria-label={choice.title} onClick={() => onSelect(choice.goal)}><CardContent><Chip label={choice.subject} size="small" /><Typography sx={{ mt: 1, fontWeight: 700 }}>{choice.title}</Typography><Typography variant="body2" color="text.secondary">{choice.description}</Typography></CardContent></CardActionArea></Card>)}</Stack></Stack> }
