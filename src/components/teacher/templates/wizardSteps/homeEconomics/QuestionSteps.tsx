import { Button, Stack, TextField, Typography } from '@mui/material'
import { NumberInput } from '../../../../lessonInputs/NumberInput'
import { SingleChoiceInput } from '../../../../lessonInputs/SingleChoiceInput'
import type { WizardStepProps } from '../../GuidedBuilderWizard'
const defaults = { mainObjective: '', lessonDurationMinutes: 50, studentCount: 30, deviceEnvironment: 'ONE_PER_STUDENT', teamMode: 'INDIVIDUAL', readingDepth: 'STANDARD', theme: '', difficulty: 'STANDARD', lifeStageFocus: 'INDEPENDENT', courseFormat: 'COMMON_CONDITIONS', roundYears: 5, coveredConcepts: [], eventDisclosure: 'ANNOUNCED', evaluationFocus: 'STABILITY' }
export function HomeEconomicsQuestionStep({ value, onChange, onNext, onBack }: WizardStepProps) { const a = { ...defaults, ...value }; const set = (patch: object) => onChange({ ...a, ...patch }); return <Stack spacing={2}><Typography variant="h6">授業の内容を教えてください（家庭科）</Typography><TextField label="主な学習目標" value={String(a.mainObjective)} onChange={(e) => set({ mainObjective: e.target.value })} /><TextField label="テーマ" value={String(a.theme)} onChange={(e) => set({ theme: e.target.value })} /><NumberInput id="lesson-duration" label="授業時間（分）" config={{ type: 'NUMBER', min: 10, max: 200 }} value={a.lessonDurationMinutes as number} errors={[]} onChange={(lessonDurationMinutes) => set({ lessonDurationMinutes })} /><SingleChoiceInput id="course-format" label="共通条件か人物比較か" config={{ type: 'SINGLE_CHOICE', options: ['COMMON_CONDITIONS', 'ROLE_VARIANT'] }} value={a.courseFormat as string} errors={[]} onChange={(courseFormat) => set({ courseFormat })} /><SingleChoiceInput id="round-years" label="1ラウンドの期間" config={{ type: 'SINGLE_CHOICE', options: ['1', '5'] }} value={String(a.roundYears)} errors={[]} onChange={(roundYears) => set({ roundYears: Number(roundYears) })} /><SingleChoiceInput id="event-disclosure" label="イベントの公開方法" config={{ type: 'SINGLE_CHOICE', options: ['ANNOUNCED', 'PARTIALLY_ANNOUNCED', 'HIDDEN'] }} value={a.eventDisclosure as string} errors={[]} onChange={(eventDisclosure) => set({ eventDisclosure })} /><Stack direction="row" spacing={1}><Button onClick={onBack}>戻る</Button><Button variant="contained" onClick={() => { onChange(a); onNext() }}>次へ</Button></Stack></Stack> }
