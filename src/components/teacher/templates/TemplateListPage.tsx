import { Button, CircularProgress, List, ListItemButton, ListItemText, Stack, Typography } from '@mui/material'
import type { LessonTemplate } from '../../../lib/lessonTemplates/types'
export function TemplateListPage({ templates, loading, onCreateNew, onOpen }: { templates: LessonTemplate[]; loading: boolean; onCreateNew: () => void; onOpen: (id: string) => void }) { return <Stack spacing={2} sx={{ p: 2 }}><Stack direction="row" sx={{ alignItems: 'center', justifyContent: 'space-between' }}><Typography variant="h5">教材一覧</Typography><Button variant="contained" onClick={onCreateNew}>新規作成</Button></Stack>{loading ? <CircularProgress aria-label="読み込み中" /> : templates.length ? <List>{templates.map((template) => <ListItemButton key={template.id} onClick={() => onOpen(template.id)}><ListItemText primary={template.draft.title} secondary={`${template.status}${template.approvalStatus === 'PENDING' ? ' ・承認待ち' : template.approvalStatus === 'REJECTED' ? ' ・却下' : ''}`} /></ListItemButton>)}</List> : <Typography color="text.secondary">まだ教材がありません。</Typography>}</Stack> }
