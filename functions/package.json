{
  "name": "functions",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "npm --prefix packages/deterministic-random run build && npm --prefix packages/lesson-runtime-types run build && npm --prefix packages/lesson-inputs run build && npm --prefix packages/market-public-content run build && npm --prefix packages/market-authoring-content run build && npm --prefix packages/household-public-content run build && npm --prefix packages/household-authoring-content run build && tsc",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src",
    "pretest": "npm --prefix packages/deterministic-random run build && npm --prefix packages/lesson-runtime-types run build && npm --prefix packages/lesson-inputs run build && npm --prefix packages/market-public-content run build && npm --prefix packages/market-authoring-content run build && npm --prefix packages/household-public-content run build && npm --prefix packages/household-authoring-content run build",
    "test": "vitest run",
    "test:market-concurrency": "vitest run --config vitest.concurrent.config.mts",
    "verify": "npm run lint && npm run typecheck && npm test && npm run build",
    "migrate:ai-beta-legacy:dry-run": "npm run build && node lib/ai/migrateLegacyBetaAccess.js --dry-run",
    "migrate:ai-beta-legacy:apply": "npm run build && node lib/ai/migrateLegacyBetaAccess.js --apply"
  },
  "dependencies": {
    "@stock-league/deterministic-random": "file:packages/deterministic-random",
    "@stock-league/household-authoring-content": "file:packages/household-authoring-content",
    "@stock-league/household-public-content": "file:packages/household-public-content",
    "@stock-league/lesson-inputs": "file:packages/lesson-inputs",
    "@stock-league/lesson-runtime-types": "file:packages/lesson-runtime-types",
    "@stock-league/market-authoring-content": "file:packages/market-authoring-content",
    "@stock-league/market-public-content": "file:packages/market-public-content",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "oxlint": "^1.71.0",
    "typescript": "~6.0.2",
    "vitest": "^4.1.10"
  }
}
